# Requirements for project-level venv
# Used by src/ modules (generation, comparison, dataset building)
# This venv is separate from the sd-scripts venvs used for training

# Core dependencies
python-dotenv==1.2.1
PyYAML==6.0.3
Pillow>=10.0.0

# For image generation and comparison (src/generate.py, src/comparison.py)
# Using newer diffusers for FluxPipeline support (SD 1.5, SDXL, FLUX)
torch>=2.0.0
torchvision>=0.15.0
diffusers>=0.30.0
transformers>=4.44.0
accelerate>=0.20.0

# Newer diffusers needs newer huggingface-hub
huggingface-hub>=0.23.2

protobuf
sentencepiece
peft